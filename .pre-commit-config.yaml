repos:
-   repo: local
    hooks:
    -   id: pytest-check
        name: pytest-check
        entry: pytest tests/ --ignore=tests/ui
        language: system
        pass_filenames: false
        always_run: true
